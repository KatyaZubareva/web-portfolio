# Отчёт по лабораторной работе: Развёртывание Hugo-сайта на GitHub Pages

## 1. Что было сделано

В рамках данного задания были выполнены следующие действия:

1. Изучены материалы по развертыванию статических сайтов через GitHub Pages.  
2. Установлена CMS **Hugo** локально.  
3. Создан новый Hugo-проект, добавлены две страницы контента:
   - `content/about.md` — страница «Обо мне»  
   - `content/projects.md` — страница с описанием проектов  
4. Локально выполнена сборка сайта командой `hugo` — результат появился в каталоге `public`.  
5. Результат сборки загружен вручную на GitHub Pages (отдельный репозиторий для Pages или через ветку `gh-pages`).  
6. Исходники Hugo-проекта размещены в отдельном репозитории на GitHub.  
7. Настроен **GitHub Actions** для автоматической сборки Hugo-сайта и публикации на GitHub Pages при каждом пуше.

---

## 2. Репозиторий с исходниками

Исходники Hugo-проекта находятся здесь:  

👉 [https://github.com/KatyaZubareva/web-portfolio](https://github.com/KatyaZubareva/web-portfolio)

---

## 3. Развёрнутый GitHub Pages сайт

Сайт-портфолио доступен по адресу:  

👉 [https://katyazubareva.github.io/web-portfolio/](https://katyazubareva.github.io/web-portfolio/)

---

## 4. Скрипт GitHub Actions для деплоя Hugo

Файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: "latest"
          extended: true

      - name: Build site
        run: hugo --minify

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

### Описание workflow

- **Checkout source** — клонирует исходники репозитория.  
- **Setup Hugo** — устанавливает Hugo на виртуальную машину.  
- **Build site** — собирает статический сайт в папку `public`.  
- **Upload artifact** — передает сгенерированный сайт в GitHub Pages.  
- **Deploy to GitHub Pages** — публикует сайт автоматически.

---

## 5. Тема и контент сайта

Для оформления выбранная тема: **PaperMod**.  
Сайт содержит страницы с большим количеством контента:

- **About Me** — описание автора, опыта и навыков  
- **Projects** — примеры проектов с деталями

---

## 6. Инструкция для проверки работы

1. Открыть репозиторий с исходниками Hugo.  
2. Проверить наличие папки `content` и `.github/workflows/deploy.yml`.  
3. Сделать изменения и выполнить `git push`.  
4. Через GitHub Actions сайт будет автоматически собран и опубликован на GitHub Pages.  

---

*Отчёт подготовлен для демонстрации развертывания статического сайта на Hugo с автоматическим деплоем через GitHub Actions.*
